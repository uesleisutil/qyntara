[extend]
# Use default gitleaks rules

[allowlist]
description = "Allow API Gateway key in dashboard config (rate-limited, non-secret)"
paths = [
  '''dashboard/src/config\.js''',
  '''\.env\.example''',
  '''\.env\.local\.example''',
]
